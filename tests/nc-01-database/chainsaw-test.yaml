apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: nc-01-database
  labels: { suite: nextcloud }
spec:
  steps:
    - try:
        - script:
            # Server-side dry-run: full admission + schema + quantity validation
            # without scheduling the (heavy) MariaDB/Nextcloud workloads.
            content: |
              kubectl apply --dry-run=server -f ../../solutions/nextcloud-casestudy/01-database-without-secrets/db.yaml
            check:
              ($error == null): true
