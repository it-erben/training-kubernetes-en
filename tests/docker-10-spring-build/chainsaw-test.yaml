apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: docker-10-spring-build
  labels: { suite: docker }
spec:
  steps:
    - try:
        - script:
            content: |
              docker buildx build --check -f ../../solutions/docker/lab-10-spring-boot-dockerfile-build/Dockerfile ../../solutions/docker/lab-10-spring-boot-dockerfile-build
            check:
              ($error == null): true
