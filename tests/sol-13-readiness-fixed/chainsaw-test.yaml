apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: sol-13-readiness-fixed
  labels: { suite: solutions }
spec:
  steps:
    - try:
        - apply: { file: ../../solutions/kubernetes/lab-13-readiness-probe/manifest-fixed.yaml }
        - assert:
            resource:
              apiVersion: apps/v1
              kind: Deployment
              metadata: { name: readiness-check-demo-deploy }
              status: { availableReplicas: 1 }
