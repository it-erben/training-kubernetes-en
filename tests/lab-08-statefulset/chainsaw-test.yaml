apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: lab-08-statefulset
  labels: { suite: labs }
spec:
  steps:
    - try:
        - apply: { file: ../../labs/kubernetes/lab-08-stateful-set-headless-service/manifest.yaml }
        - assert:
            resource:
              apiVersion: apps/v1
              kind: StatefulSet
              metadata: { name: nginx }
              status: { readyReplicas: 3 }
