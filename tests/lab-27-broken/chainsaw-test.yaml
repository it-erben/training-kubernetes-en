apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: lab-27-broken
  labels: { suite: labs }
spec:
  # Every workload in this lab is broken by design. Asserting only "not ready"
  # would still pass if a scenario broke for the wrong reason (a pulled tag
  # disappearing, say), so each one is pinned to its own symptom.
  timeouts:
    assert: 300s
  steps:
    - try:
        - script:
            content: |
              set -eu
              kubectl apply -n "$NAMESPACE" \
                -f ../../labs/kubernetes/lab-27-troubleshooting/setup.yaml

        # 1. cache: secretKeyRef names a key the Secret does not have, so the
        #    kubelet cannot build the container's environment and it is never
        #    created.
        - assert:
            resource:
              apiVersion: v1
              kind: Pod
              metadata:
                labels: { app: cache }
              status:
                containerStatuses:
                  - state:
                      waiting:
                        reason: CreateContainerConfigError

        # 2. sessions: the container starts, finds REDIS_HOST unset, and
        #    exits 1, so it crashloops.
        - assert:
            resource:
              apiVersion: v1
              kind: Pod
              metadata:
                labels: { app: sessions }
              status:
                containerStatuses:
                  - state:
                      waiting:
                        reason: CrashLoopBackOff
                    lastState:
                      terminated:
                        exitCode: 1

        # 3. reports: requests 64Gi of memory, so no node can take it.
        - assert:
            resource:
              apiVersion: v1
              kind: Pod
              metadata:
                labels: { app: reports }
              status:
                phase: Pending

        # 4. crunch: writes 100Mi into a memory-backed emptyDir under a 64Mi
        #    limit, so the kernel kills it. The first kill leaves its written
        #    pages resident in the pod-scoped emptyDir, so every kubelet
        #    restart after that OOMs again before the container even starts,
        #    settling into a stable CrashLoopBackOff. Pinned to that shape,
        #    not a bare "not ready", because a workload that merely restarts
        #    for an unrelated reason would pass a weaker assertion.
        - assert:
            resource:
              apiVersion: v1
              kind: Pod
              metadata:
                labels: { app: crunch }
              status:
                containerStatuses:
                  - state:
                      waiting:
                        reason: CrashLoopBackOff
                    lastState:
                      terminated:
                        reason: StartError
                        exitCode: 128
