apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: sol-03-loop-pod-date
  labels: { suite: solutions }
spec:
  steps:
    - try:
        - apply: { file: ../../solutions/kubernetes/lab-03-pods-challenge/loop-pod-date.yaml }
        - assert:
            resource:
              apiVersion: v1
              kind: Pod
              metadata: { name: loop }
              status:
                phase: Running
                (conditions[?type == 'Ready'] | [0].status): "True"
