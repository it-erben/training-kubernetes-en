apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: lab-06-rolling-update
  labels: { suite: labs }
spec:
  steps:
    - try:
        - apply: { file: ../../labs/kubernetes/lab-06-deployment-rolling-update/manifest.yaml }
        - assert:
            resource:
              apiVersion: apps/v1
              kind: Deployment
              metadata: { name: deployment-demo }
              status: { availableReplicas: 10 }
