apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: lab-05-replicaset
  labels: { suite: labs }
spec:
  steps:
    - try:
        - apply: { file: ../../labs/kubernetes/lab-05-replicaset/manifest.yaml }
        - assert:
            resource:
              apiVersion: apps/v1
              kind: ReplicaSet
              metadata: { name: replicaset-demo }
              status: { readyReplicas: 3 }
