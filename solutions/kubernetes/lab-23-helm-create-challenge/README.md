# Solution: Creating a Helm Chart

This is the reference solution for building your own Helm chart.

The chart lives in the `my-nginx-chart` subfolder.

## Installing the solution

```bash
helm install my-solution ./my-nginx-chart
```
