# Solution: Creating a Helm Chart

This is the reference solution for the task of creating your own Helm chart.

The chart is located in the `my-nginx-chart` subfolder.

## Installing the solution

```bash
helm install my-solution ./my-nginx-chart
```
